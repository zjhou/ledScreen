{
  "name": "demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "microbundle -i js/demo.js -o dist",
    "dev": "microbundle watch -i js/demo.js -o dist --format cjs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "microbundle": "^0.2.4"
  }
}
